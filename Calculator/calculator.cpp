#include "calculator.h"

Calculator::Calculator() {
    std::vector<Operation*> add_sub;
    add_sub.push_back(new Addition());
    operations.insert(make_pair(priority::ADD_SUB, new std::vector.))
}
